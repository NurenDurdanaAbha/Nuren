#include<iostream>

using namespace std;

int main()
{
     while(1)
    {
        gets(s);
        int count=0, count2=0;

        int l=strlen(s);
        for(int i=0; i<l;i++)
        {
            if(s[i]=='a')
                a++;
        }
    }

    return 0;
}
